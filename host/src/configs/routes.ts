import path, { resolve } from 'path';

const CONTEXT = '../../src/pages/';

export default (function (routes) {
  // routes.push({
  //   name: 'cmn-menu-mgt',
  //   path: '/cmn/menu/mgt',
  //   file: resolve(__dirname, `${CONTEXT}cmn/menu/mgt/CSM0002M00.vue`),
  // });
});
